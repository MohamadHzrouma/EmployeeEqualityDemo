namespace IQuittableDemo
{
    // Define an interface named IQuittable
    // An interface is like a contract — any class that implements it
    // must provide a definition for all its methods
    public interface IQuittable
    {
        // Method signature for Quit() — no implementation here
        void Quit();
    }
}